fastapi==0.115.12
uvicorn[standard]==0.34.2
maxminddb==3.1.1          # MMDB reader (mmap-based, low RSS)
mmdb-writer==0.2.7        # MMDB writer (build .mmdb from CIDR+value pairs)
python-multipart==0.0.20
python-dotenv==1.1.0
cabby==0.1.23            # OTX TAXII 1.x client (AlienVault OTX bulk download)
pymispwarninglists>=1.7.0
orjson==3.11.9           # fast JSON for NDJSON stream serialization
psutil==6.1.0            # cross-platform memory stats for rebuild valve
# Optional: STIX 2.1 export support
# stix2>=3.0.1
